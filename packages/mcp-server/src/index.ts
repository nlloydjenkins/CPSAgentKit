// @agent-workbench/mcp-server — MCP server scaffold.
// Tool implementations will be added in milestones M3+.
// Transports: stdio (default) and HTTP (planned).

export type {
  AssessmentReport,
  AssessmentScope,
  AgentConfig,
  KnowledgeDocument,
  KnowledgeTopic,
} from "@agent-workbench/core";

export const MCP_SERVER_VERSION = "1.0.0";
